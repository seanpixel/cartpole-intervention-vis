# core
streamlit==1.45.1

# numerics & plotting
numpy==1.23.5
pandas==2.3.0
matplotlib==3.10.3

# Gym (CartPole doesn’t require Box2D, but you’ll need pyglet for rendering)
gym==0.26.2
pyglet==2.0.9

# PyTorch — pick the latest 2.x CPU wheel
torch==2.0.1
